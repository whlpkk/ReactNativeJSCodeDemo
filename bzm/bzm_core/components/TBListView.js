'use strict';

var React = require('react-native');

// var ListView = require('ListView');
var ListView = require('View');
class TBListView extends ListView {
  setObject(obj) {

  }
  index() {

  }

  setIndex(inx) {

  }
}
module.exports = TBListView;
